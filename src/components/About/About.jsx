export default function About({ title, subtitle, description }) {

    return (
        <div>
            <h1>{title}</h1>

            <h3>{subtitle}</h3>

            <p>{description}</p>
        </div>
    )
}